generator: admingenerator.generator.doctrine
params:
  model: TC\IndexBundle\Entity\Item
  namespace_prefix: TC
  bundle_name: IndexBundle
  fields: ~

builders:
  list:
    params:
      title: Items list
      display: [title, url, category]
      actions:
        new: ~
      object_actions:
        edit: ~
        delete: ~
  filters:
    params:
      display: [title, url, category]
  new:
    params:
      title: New item
      display: [title, url, synopsis, category]
      actions:
        list: ~
  edit:
    params:
      title: You're editing the item "{{ Item.title }}"
      display: [title, url, synopsis, category]
      actions:
        list: ~
  delete: ~
