{% extends "TCIndexBundle:Default:_main.html.twig" %}

{% block title %}Importation d'index{% endblock %}

{% block body %}
<p>
    Quelle importation souhaitez-vous lancer ? 
</p>
<ul>
    <li>
        <form action="{{ path("tc_index_importer_htmlfileimport") }}" method="get">
            Importer un fichier HTML (chemin relativement à la racine du domaine) : <input type="text" name="page"/>. <input type="submit" value="Importer" />
        </form>
    </li>
    <li>
        <a href="{{ path("tc_index_importer_xmlcategoriesimport") }}">Importer les catégories à partir de l'arborescence, la racine étant <code>tutoriels</code> relativement à la racine du domaine. </a>
    </li>
    <li>
        <a href="{{ path("tc_index_importer_xmlarticlesimport") }}">Importer les articles à partir de l'arborescence, la racine étant <code>tutoriels</code> relativement à la racine du domaine. </a>
    </li>
</ul>
{% endblock %}